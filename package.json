{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest --watch",
    "test:ci": "jest --ci --coverage --maxWorkers=2 --reporters=jest-standard-reporter"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-junit": "^13.0.0",
    "jest-standard-reporter": "^2.0.0",
    "jest-watch-typeahead": "^1.0.0",
    "lerna": "^5.6.2",
    "prettier": "^2.1.2",
    "semver": "^7.5.4"
  }
}
